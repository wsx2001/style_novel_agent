# FictionForge backend dependencies
# Install: cd backend && pip install -r requirements.txt
# Use >= lower bound to pin major versions; test before upgrading (docs/TECH.md sec 10)

fastapi>=0.115
uvicorn[standard]>=0.34
SQLAlchemy[asyncio]>=2.0.36
aiosqlite>=0.20
pydantic-settings>=2.7
python-multipart>=0.0.20
openai>=1.59
# chroma 0.6.x：1.x 的 Rust 写入路径在 Windows 上崩溃；0.6.3 为纯 Python 实现
chromadb>=0.6.3,<1.0
# onnxruntime：1.28 在部分 Windows 上 DLL 加载失败，锁定 1.20.x 稳定版
onnxruntime>=1.20,<1.28
python-docx>=1.1.2
Markdown>=3.7
cryptography>=44.0
alembic>=1.14
httpx>=0.28
